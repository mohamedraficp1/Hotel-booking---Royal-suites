
               <!-- dashboard inner -->

               <div class="add-room">
                  <a type="button" href="/vendor/add-room" style="width: 100px" class="btn btn-primary mt-5 ml-auto d-block">Add Room</a>  
               </div>

                <h2 class="text-center mt-5 pt-5">Rooms</h2>
                 <table class="display table" id="table_id">
        <thead>
          <tr>
            <th>Sl No</th>
            <th>Rooms</th>
            <th>Image</th>
            <th>Edit</th>
          </tr>
        </thead>
        <tbody>
        {{#each roomData.rooms}}
            <tr>
              <td>{{inc @index}}</td>
             
                 <td>{{this.roomName}}</td>
                 {{!-- {{#each this.img}} --}}
                  <td><img src="/images/room-images/{{this.img.[0].filename}}" width="100"></td>
                  {{!-- {{/each}} --}}
               <td>
               <a  href="/vendor/edit-room/{{this._id}}" class="btn btn-primary d-inline-block" >
  Edit Room
</a>
              <a href="/vendor/delete-room/{{this._id}}" class="btn btn-danger">Delete</a></td>
              </form>
            </tr>
              <!-- Modal -->

            {{/each}}


              
               
               <!-- end dashboard inner -->
            </div>
         </div>
      </div>

      <script>

         $(document).ready( function () {
            $('#table_id').wrap('<div id="hide" style="display:none"/>');
                  $('#hide').css( 'display', 'block' );
            } );
         var table = $('#table_id').DataTable();
      </script>
      